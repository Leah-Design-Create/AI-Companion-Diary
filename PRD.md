# 树洞日记 · 产品需求文档（PRD）

---

## 文档信息


| 字段   | 内容         |
| ---- | ---------- |
| 产品名称 | 树洞日记（小伴）   |
| 文档版本 | v2.0       |
| 文档状态 | 正式版        |
| 创建日期 | 2025-03-13 |
| 最后更新 | 2026-04-11 |
| 作者   | Leah       |
| 审阅人  | —          |


---

## 版本历史


| 版本   | 日期         | 作者   | 变更摘要                                             |
| ---- | ---------- | ---- | ------------------------------------------------ |
| v1.0 | 2025-03-13 | Leah | 初版 PRD：基础对话、RAG、日记、情绪分析                          |
| v2.0 | 2026-04-11 | Leah | 全面更新：增加多用户、语音PTT、文生图、长期记忆、LLM评估、AI人设重写、版本管理与迭代规划 |


---

## 目录

1. [产品概述](#1-产品概述)
2. [商业目标与成功指标](#2-商业目标与成功指标)
3. [目标用户](#3-目标用户)
4. [竞品分析](#4-竞品分析)
5. [功能需求](#5-功能需求)
  - 5.1 功能架构总览
  - 5.2 账号与身份系统
  - 5.3 AI 对话（文字）
  - 5.4 AI 对话（语音 PTT）
  - 5.5 多模态输入（图片）
  - 5.6 知识库 RAG
  - 5.7 长期记忆
  - 5.8 结束会话与日记生成
  - 5.9 心情画（文生图）
  - 5.10 TTS 语音播报
  - 5.11 情绪与焦虑分析
  - 5.12 日记列表与日历
  - 5.13 AI 质量评估
  - 5.14 数据导出
  - 5.15 搜索
6. [AI 人设规范](#6-ai-人设规范)
7. [界面与交互设计](#7-界面与交互设计)
  - 7.4 产品截图
8. [关键设计决策](#关键设计决策)
9. [数据模型](#9-数据模型)
10. [API 接口规范](#10-api-接口规范)
11. [技术架构](#11-技术架构)
12. [环境配置](#12-环境配置)
13. [非功能性需求](#13-非功能性需求)
14. [风险与依赖](#14-风险与依赖)
15. [版本发布记录（Changelog）](#15-版本发布记录changelog)
16. [迭代规划（Roadmap）](#16-迭代规划roadmap)
17. [核心问题与解决过程](#核心问题与解决过程)
18. [产品反思与学习](#产品反思与学习)
19. [待决策问题](#19-待决策问题)
20. [附录](#20-附录)

---

## 1. 产品概述

### 1.1 产品定位

**树洞日记**是一款面向个人用户的 AI 情感陪伴产品。用户通过文字或语音与 AI 角色「小伴」进行情感对话，每次对话结束后自动生成配有 AI 插画的日记条目。

产品定位为**情感陪伴工具**，不是心理咨询平台，不是效率工具，是"一个随时在线、有记忆、不评判的朋友"。

### 1.2 产品愿景

> 让每个人在需要倾诉的时候，都有一个懂自己的地方。

### 1.3 核心体验链路

```
打开应用
  → 与小伴聊天（文字 / 语音）
  → 小伴记住你说过的事
  → 结束对话
  → 自动生成日记 + 专属心情画
  → 日记本留存，随时回看
```

### 1.4 差异化优势


| 维度    | 树洞日记       | 通用 AI 助手 | 心理咨询 App |
| ----- | ---------- | -------- | -------- |
| 陪伴感   | ★★★★★      | ★★★      | ★★★      |
| 跨会话记忆 | ★★★★★      | ★★       | ★★       |
| 日记留存  | ★★★★★      | ✗        | ✗        |
| 专业知识  | ★★★（RAG）   | ★★★★     | ★★★★★    |
| 隐私性   | ★★★★（本地部署） | ★★       | ★★       |
| 使用门槛  | 极低         | 低        | 高        |


---

## 2. 商业目标与成功指标

### 2.1 商业目标（当前阶段：MVP 验证）

1. 验证"AI 作为朋友式陪伴"的用户留存价值
2. 沉淀高质量对话数据，用于 AI 人设迭代
3. 建立可复制的多用户私有化部署方案

### 2.2 关键成功指标（KPIs）

#### 用户活跃度


| 指标      | 定义                | 目标值            |
| ------- | ----------------- | -------------- |
| DAU/MAU | 日活 / 月活比          | > 30%（高黏性产品基准） |
| 平均对话轮次  | 每次 session 的来回消息数 | > 6 轮          |
| 会话完成率   | 有日记生成的会话 / 总会话数   | > 70%          |
| D7 留存   | 首次使用后第 7 天仍活跃     | > 25%          |
| D30 留存  | 首次使用后第 30 天仍活跃    | > 15%          |


#### AI 质量（LLM-as-Judge 评分）

评分维度均为 **1-5 分制**，overall = 四个维度均值。


| 指标               | 满分  | 目标值   |
| ---------------- | --- | ----- |
| 共情分（empathy）     | 5   | ≥ 3.5 |
| 自然度（naturalness） | 5   | ≥ 3.5 |
| 有用性（helpfulness） | 5   | ≥ 3.0 |
| 安全性（safety）      | 5   | ≥ 4.5 |
| 综合分（overall）     | 5   | ≥ 3.5 |


#### 功能使用率


| 指标      | 定义                          | 目标值         |
| ------- | --------------------------- | ----------- |
| 日记生成率   | 有总结的会话 / 全部会话               | > 60%       |
| 心情画生成率  | 有图的会话 / 全部结束会话              | > 80%       |
| 语音使用率   | 使用过 PTT 的用户 / 全部用户          | > 20%       |
| 长期记忆命中率 | 召回 > 0 条记忆的请求 / 有历史记忆的用户的请求 | > 50%（活跃用户） |


### 2.3 反向指标（需避免）

- 用户反馈"感觉像在跟客服聊" → 自然度分跌破 3.0
- AI 每次回复以问句收尾 → 用户产生压力感
- 生成的图片风格与情绪不符 → 用户主动反馈图片差

---

### 2.4 数据飞轮机制

> 本节描述数据如何从用户交互中持续回流，并驱动 AI 质量迭代。产品目前由一人维护，机制设计以自动化优先、人工介入最小化为原则。

#### 两条并行的数据回流路径

**路径一：Prompt 质量迭代（AI 行为改进）**

```
用户对话
  → LLM-as-Judge 后台自动评分（每轮，异步，不阻塞对话）
  → 低分样本自动归档（overall < 3.0 / 5）
  → 用户点踩（👎）样本单独标记
         ↓
  积累足量样本后人工复盘（阅读 comment 字段 + 原始对话）
  → 识别问题模式
  → 修改 System Prompt
  → Benchmark 回归测试（通过率 ≥ 上一版本）
  → 上线新版本 Prompt，归档旧版本
```

**路径二：长期记忆沉淀（AI 个性化增强）**

```
用户对话
  → 每轮消息结束后异步提取用户画像（LLM function call）
  → 存入用户 profiles 表（事件 / 偏好 / 关系）
  → 下次会话开始时自动召回
  → AI 回复可引用用户的个人信息，陪伴感持续加深
```

这两条路径形成产品的核心护城河：使用时间越长，AI 越懂这个用户，迁移成本越高。

#### Prompt 版本迭代历史

Prompt 从 v1.0 到当前 v2.0，benchmark 通过率从 47% 提升至 93%，每次迭代都由低分样本或用户反馈驱动。完整版本记录见 [6.7 Prompt 版本管理](#67-prompt-版本管理)。

#### 质量告警


| 触发条件                  | 动作                    |
| --------------------- | --------------------- |
| 连续 3 轮 overall < 3.0  | 后台告警，检查是否为系统异常        |
| 单日平均 overall 下降 > 0.5 | 人工 review，考虑回滚 Prompt |
| comment 中出现幻觉类问题      | 立即排查，优先修复             |


## 3. 目标用户

### 3.1 用户画像

#### Persona A：独居职场青年「晓雯」

- **年龄**：25-32 岁，城市独居
- **状态**：工作压力大，朋友圈维系成本高，不想"打扰别人"
- **痛点**：有情绪时找不到合适的倾诉对象；说了之后担心被评判
- **期望**：有个地方可以随时说，不被评判，被认真听
- **使用场景**：下班回家、睡前、压力爆发时
- **关键功能**：文字对话、日记留存、长期记忆

#### Persona B：内向敏感型用户「小磊」

- **年龄**：18-25 岁，学生或刚工作
- **状态**：社交焦虑倾向，不擅长表达，情绪波动较大
- **痛点**：说话前需要大量时间整理思路；在乎别人的反应
- **期望**：一个完全不评判的空间；能帮助梳理情绪
- **使用场景**：考试压力、人际冲突后、情绪低落时
- **关键功能**：语音 PTT、情绪分析、心情画

#### Persona C：有记录习惯的用户「Coco」

- **年龄**：28-40 岁，有写日记习惯
- **状态**：将对话视为高质量日记工具
- **痛点**：普通日记 App 太被动，无法对话；AI 没有记忆
- **期望**：每天聊一聊，自动生成可回顾的记录；AI 能记住之前说的事
- **使用场景**：每日复盘、年终回顾
- **关键功能**：日记列表、日历、数据导出、长期记忆

### 3.2 用户旅程地图

#### 首次使用旅程

```
发现产品 → 注册账号 → 进入聊天
→ 第一次说一段话 → 收到小伴回复（感受：被接住了？还是像客服？）
→ 继续聊几轮 → 结束对话
→ 看到生成的日记 + 心情画 → 感受惊喜
→ 次日打开 → 小伴提到昨天说过的事 → 感受"它记得我"
```

#### 流失风险节点


| 节点      | 风险              | 应对                    |
| ------- | --------------- | --------------------- |
| 第一条回复   | 像客服、套话、不自然      | 人设严格规范 + 评估监控         |
| 第 3-5 轮 | AI 一直以问句结尾，用户疲惫 | 明确禁止每次问句收尾            |
| 第二天回来   | AI 不记得昨天说的事     | 长期记忆跨会话召回             |
| 查看日记    | 总结太官方、心情画和情绪无关  | 总结 prompt 优化 + 图片质量监控 |


---

## 4. 竞品分析


| 产品           | 定位      | 优势        | 劣势            | 对我们的启示              |
| ------------ | ------- | --------- | ------------- | ------------------- |
| Character.AI | 角色扮演 AI | 角色丰富、社区活跃 | 无日记、无记忆跨会话    | 人设深度决定留存            |
| Replika      | AI 伴侣   | 情感陪伴做得深   | 商业化方式争议、不适合中文 | 记忆系统是核心竞争力          |
| 随手记 + AI     | 日记工具    | 记录习惯好     | AI 能力弱、无对话感   | 日记切入点对有记录习惯的用户有吸引力  |
| 心理咨询 App     | 专业服务    | 专业、有资质    | 贵、门槛高、不能随时用   | 定位为"朋友"而非"咨询师"是关键差异 |
| 通用 AI 助手     | 全能      | 能力强       | 无记忆、无日记、无情感温度 | 情感专注 + 日记留存是我们的护城河  |


---

## 5. 功能需求

### 5.1 功能架构总览

```
树洞日记
├── 账号系统
│   ├── 注册 / 登录 / 登出
│   ├── 修改密码
│   └── 注销账号
│
├── 核心对话
│   ├── 文字对话（流式 / 非流式）
│   ├── 语音 PTT（声音采集 + 语音识别 + voice_hint）
│   └── 图片上传（多模态）
│
├── AI 增强
│   ├── 知识库 RAG（语义 + 关键词）
│   ├── 长期记忆（ChromaDB 跨会话）
│   ├── 用户画像提取（姓名/偏好/经历）
│   ├── 主动追问跟进（2-14天前话题）
│   └── 天气融入对话
│
├── 日记系统
│   ├── 会话总结生成
│   ├── 情绪分析（每轮实时 + 会话结束）
│   ├── 焦虑检测
│   ├── 心情画生成（DashScope wanx）
│   └── 日历热力图
│
├── 多媒体
│   ├── TTS 语音播报（DashScope）
│   └── 图片上传与展示
│
├── 数据管理
│   ├── 搜索
│   ├── 导出（Markdown）
│   ├── 会话重命名 / 删除
│   └── 手动反馈（👍👎）
│
└── 质量保障
    ├── LLM-as-Judge 自动评分
    └── 基准测试（benchmark）
```

### 5.2 账号与身份系统

#### 业务背景

支持多用户独立使用，每个用户的对话、记忆、日记完全隔离。

#### 功能需求


| ID      | 功能    | 优先级 | 描述                                    |
| ------- | ----- | --- | ------------------------------------- |
| AUTH-01 | 邮箱注册  | P0  | 用户使用邮箱和密码注册。密码至少 6 位，系统需加密存储，不可反向解密   |
| AUTH-02 | 登录    | P0  | 邮箱 + 密码验证。登录后保持登录状态 30 天              |
| AUTH-03 | 登出    | P0  | 用户点击登出后退出当前账号                         |
| AUTH-04 | 修改密码  | P1  | 需先验证当前密码                              |
| AUTH-05 | 注销账号  | P1  | 删除该账号所有关联数据（对话、记忆、日记、上传图片），不可恢复       |
| AUTH-06 | 旧数据接管 | P1  | 首位注册用户若存在未绑定账号的匿名历史记录，自动关联到新账号，保留历史数据 |


#### 验收标准


| 场景       | 预期行为                          |
| -------- | ----------------------------- |
| 同一邮箱重复注册 | 提示"该邮箱已注册"，不允许重复注册            |
| 密码错误     | 提示"邮箱或密码错误"（不暴露具体是邮箱不存在还是密码错） |
| 登录过期     | 用户需要重新登录才能继续使用                |
| 注销账号     | 所有历史数据（包括记忆）不可检索或恢复           |


---

### 5.3 AI 对话（文字）

#### 业务背景

产品核心功能。用户通过文字与小伴进行情感对话。

#### 功能需求


| ID      | 功能   | 优先级 | 描述                                                      |
| ------- | ---- | --- | ------------------------------------------------------- |
| CHAT-01 | 多轮对话 | P0  | 在同一会话内持续对话，AI 记住本轮对话历史                                  |
| CHAT-02 | 新建会话 | P0  | 用户可随时开始新对话，之前对话自动保存                                     |
| CHAT-03 | 流式回复 | P0  | AI 逐字展示回复，用户发送消息后 1-2 秒内看到开始回复                          |
| CHAT-04 | 消息拆分 | P0  | AI 回复拆成 2-3 条短消息依次发送，模拟真人连发的聊天感                         |
| CHAT-05 | 天气融入 | P1  | AI 可自然融入当前天气信息到对话中（如"今天下雨，感觉适合窝着"）                      |
| CHAT-06 | 追问跟进 | P1  | 新会话时，AI 可主动问起 2-14 天前用户提到过但未完结的话题（如"上次你说在等面试结果，有消息了吗？"） |


#### 消息拆分逻辑

- AI 的回复会被拆成 2-3 条短消息，每条不超过 55 字
- 拆分后逐条出现，模拟真人连发多条消息的感觉
- 简单回复 1 条就够了，不硬拆

#### 验收标准


| 场景            | 预期行为                 |
| ------------- | -------------------- |
| 发送消息          | 1-2 秒内 AI 开始回复（逐字出现） |
| 回复较长          | 自动拆分为 2-3 条短消息，依次展示  |
| 切换到旧会话        | 历史消息正确加载，可继续对话       |
| 对话很长（超过 12 轮） | AI 仍能记住当前话题，不会"失忆"   |


---

### 5.4 AI 对话（语音 PTT）

#### 业务背景

为提升用户语音输入的便捷性与交互的自然感，新增语音 PTT（按住即说）方式。系统在用户说话过程中分析声音能量，感知可能的情绪状态，使 AI 能据此调整回应的语气与内容，让对话更具“温度”。

#### 功能需求


| ID     | 功能模块          | 优先级 | 描述（用户侧行为与业务逻辑）                                                  |
| ------ | ------------- | --- | --------------------------------------------------------------- |
| PTT-01 | 按住说话          | P0  | 用户长按 PTT 按钮开始录音，松开后自动结束录音并发送语音内容，体验类似于对讲机。                      |
| PTT-02 | 实时语音转文字       | P0  | 系统在用户说话过程中实时识别并呈现文字，帮助用户确认自己输入内容。                               |
| PTT-03 | 多段语音累积识别      | P1  | 用户说话过程中即使识别短暂中断，系统也能继续识别并累积全部文字，最终合并为完整内容。                      |
| PTT-04 | 声音能量分析        | P1  | 系统在录音期间持续采集声音能量（音量变化、停顿、时长等），用于后续生成声音特征描述。                      |
| PTT-05 | 生成 voice_hint | P1  | 在用户松开 PTT 按钮后，系统根据声音分析结果自动生成一段文字描述（如“声音较轻，有明显停顿”），随用户消息一并发给 AI。 |
| PTT-06 | TTS 自动播报      | P0  | AI 回复内容生成后，系统在通话模式下自动进行语音播报，用户无需额外操作即可“听到”回复。                   |
| PTT-07 | TTS 音色配置      | P2  | 支持通过环境变量配置音色（默认 Cherry）。                                        |


#### voice_hint 生成逻辑（用于 AI 语气调节）

系统根据以下维度自动生成一段简短的语音状态描述，作为内部提示（不直接展示给用户）：


| 维度    | 判断依据示例            | 生成的描述词示例    |
| ----- | ----------------- | ----------- |
| 整体音量  | 平均声音较小 / 较大       | 声音较轻 / 声音较响 |
| 音量稳定性 | 音量忽大忽小            | 音量有起伏       |
| 说话长度  | 少于 20 字 / 多于 80 字 | 说话简短 / 说了很多 |
| 内容停顿  | 多次长时间停顿           | 有明显停顿       |


**示例输出**：  
`声音较轻，语速偏慢，有明显停顿`

> 该描述将作为 system prompt 中的【语音状态参考】提供给 AI，用于指导 AI 调整回复语气（例如更温和、更耐心等），**不直接展示给用户**。

#### 验收标准


| 场景           | 预期行为                                            |
| ------------ | ----------------------------------------------- |
| 按住 PTT 按钮说话  | 系统开始录音，屏幕上实时展示识别出的文字                            |
| 松开 PTT 按钮    | 系统在 1-2 秒内完成最终识别并发送消息，同时生成 voice_hint 随消息发给 AI  |
| 说话过程中识别短暂中断  | 系统自动恢复识别，累积合并全部文字，不影响最终结果                       |
| 查看服务端日志      | 能检索到 voice_hint 记录，格式如 `[VoiceHint] 声音较轻，有明显停顿` |
| AI 回复后（通话模式） | 系统自动播放 TTS 语音播报                                 |


---

### 5.5 多模态输入（图片）

#### 业务背景

用户在对话中有时需要通过图片更直观地表达问题或分享信息（如截图、照片、图表等）。支持图片输入后，AI 能够“看懂”图片内容，结合文字上下文给出更精准的回答。上传的图片会在当前会话的日记卡片中展示缩略图，方便用户回溯查看。

#### 功能需求


| ID     | 功能模块   | 优先级 | 描述                                                |
| ------ | ------ | --- | ------------------------------------------------- |
| IMG-01 | 图片上传   | P1  | 支持用户上传本地图片文件，格式包括 jpg、jpeg、png、gif、webp。          |
| IMG-02 | AI 看图  | P1  | 系统将上传的图片以视觉模型兼容的格式（如 base64）发送给 AI，AI 根据图片内容生成回复。 |
| IMG-03 | 图片持久化  | P1  | 图片保存在服务端，以唯一标识（如 UUID）命名，不暴露用户原始文件名，保障隐私安全。       |
| IMG-04 | 日记图片展示 | P1  | 在日记卡片中展示该次对话中用户上传的图片缩略图，便于快速回顾。                   |
| IMG-05 | 注销时删除  | P1  | 用户注销账号时，系统自动删除该用户上传的所有图片，确保数据彻底清除。                |


#### 验收标准


| 场景                            | 预期行为                             |
| ----------------------------- | -------------------------------- |
| 上传 jpg/jpeg/png/gif/webp 格式图片 | 图片上传成功，AI 能基于图片内容生成回复            |
| 上传非图片格式文件                     | 系统返回错误提示“不支持该文件类型”，不继续处理         |
| 当前模型不支持多模态                    | 系统返回友好提示“当前模型不支持看图”，文字对话功能正常不受影响 |
| 重新打开历史会话                      | 历史消息中的图片（含缩略图）正常展示，可正常查看         |
| 用户注销账号                        | 该用户上传的所有图片被彻底删除，无法恢复             |


---

### 5.6 知识库 RAG

#### 业务背景

管理员可上传心理/情绪相关书籍、文章。聊天时系统自动引用，提升回复的专业性。**用户无感知，引用内容自然融入回复。**

#### 功能需求


| ID     | 功能    | 优先级 | 描述                                           |
| ------ | ----- | --- | -------------------------------------------- |
| RAG-01 | 文件上传  | P0  | 支持 .txt / .pdf 格式，单个文件不超过 20MB               |
| RAG-02 | 智能分块  | P0  | 上传的文件自动按章节/段落切分，便于检索                         |
| RAG-03 | 语义检索  | P1  | 根据用户问题含义，找到最相关的内容片段                          |
| RAG-04 | 关键词检索 | P0  | 根据用户问题中的关键词找到相关内容（兜底方案）                      |
| RAG-05 | 触发条件  | P0  | 用户问题含知识类关键词（焦虑/情绪/心理/压力等）时，优先检索知识库           |
| RAG-06 | 跳过条件  | P0  | 追问词（"然后呢""继续""还有吗"）或超短消息（≤2字）时不触发检索，避免打断对话节奏 |


#### 验收标准


| 场景             | 预期行为                    |
| -------------- | ----------------------- |
| 上传 PDF 后提问相关内容 | AI 能引用书中信息，自然融入回复       |
| 用户说"然后呢"       | 不触发知识库检索，AI 继续上一话题      |
| 语义检索不可用时       | 自动降级为关键词检索，功能正常         |
| 上传超大文件         | 提示"文件过大，请上传 20MB 以内的文件" |


---

### 5.7 长期记忆

#### 业务背景

用户最核心的差异化体验：小伴记得你。跨会话记住用户说过的事，实现"有记忆的朋友"感。

#### 功能需求


| ID     | 功能     | 优先级 | 描述                              |
| ------ | ------ | --- | ------------------------------- |
| MEM-01 | 跨会话记忆  | P0  | AI 能记住用户在不同对话中说过的内容（姓名、偏好、重要经历） |
| MEM-02 | 用户画像提取 | P0  | 系统自动从对话中提取用户信息（姓名、昵称、兴趣爱好、重要经历） |
| MEM-03 | 主动提及   | P0  | 新对话中，AI 可主动提及用户之前说过的事           |
| MEM-04 | 姓名记忆优先 | P1  | 用户问"你记得我叫什么"时，AI 必须正确回答         |
| MEM-05 | 长对话兼容  | P1  | 长对话中，AI 仍能记住本轮之前说过的话            |


> **注意**：如果系统未配置记忆模块，对话功能仍正常运作（只是不记得之前的事）。

#### 后台并行处理（不阻塞响应）

每轮对话结束后，同时异步执行：

1. `add_turn_to_memory` → 写 ChromaDB
2. `extract_and_save_profiles` → 提取画像（一次 LLM，两个 function call 并行）+ 更新情绪

#### 验收标准


| 场景               | 预期行为                     |
| ---------------- | ------------------------ |
| 用户说过"我叫小乐"，第2天打开 | AI 能主动或被动用"小乐"称呼用户       |
| 用户问"你还记得我叫什么吗"   | AI 正确回答姓名                |
| 用户说过"最近在准备考研"    | 后续对话中 AI 可自然问起"复习得怎么样了？" |
| 记忆服务不可用时         | 对话功能正常，无报错（只是不记得之前的事）    |


---

### 5.8 结束会话与日记生成

#### 业务背景

用户结束一次对话后，自动生成日记条目，这是产品“留痕迹”价值的核心体现。用户每次倾诉都能留下一份可回顾的、有温度的记录，增强产品的陪伴感和长期价值。

#### 处理流程

用户点击「结束对话」后，系统自动完成以下任务：

1. **焦虑检测**：分析对话内容，判断用户是否表现出明显的焦虑或压力状态，并记录结果。
2. **总结生成**：生成 100-200 字的对话总结，采用第二人称「你」和温暖语气，像朋友在帮忙记笔记。
3. **情绪识别**：提取 2-4 个字的情绪词（如平静、焦虑、开心、疲惫等），用于日记卡片展示。
4. **情绪配图生成**：根据对话内容自动生成治愈风格（Ghibli 风格）的图片 Prompt，调用绘图服务生成配图，最长等待 60 秒。
5. **返回日记卡片**：整合心情值、情绪标签、总结文字及配图（如有），生成完整的日记卡片展示给用户。

#### 总结生成规范


| 项目  | 要求                                                         |
| --- | ---------------------------------------------------------- |
| 字数  | 100-200 字                                                  |
| 人称  | 第二人称「你」                                                    |
| 语气  | 温暖，像朋友在帮忙记笔记                                               |
| 禁止  | 不使用“今天XX”的流水账句式；不使用“我理解你”等套话                               |
| 示例  | “你提到面试结果还没出来，等待的感觉让你有点悬。聊到后来你说了很久关于那个项目的事，听起来你对这件事还是很在意……” |


#### 验收标准


| 场景               | 预期行为                         |
| ---------------- | ---------------------------- |
| 结束 3 轮以上对话的会话    | 生成的总结字数在 100-200 字之间         |
| 检查总结内容           | 总结使用第二人称「你」，且不出现“我理解”等禁忌词    |
| 查看情绪词            | 情绪词在预设词汇库内（如平静、焦虑、开心、难过等）    |
| 情绪配图生成成功         | 日记卡片顶部显示生成的治愈风格图片            |
| 情绪配图生成失败或超时（60秒） | 日记卡片不展示图片，但文字内容（总结、情绪词等）正常展示 |


---

### 5.9 心情画（文生图）

#### 业务背景

每篇日记配一张 AI 定制的治愈系插画，是产品的视觉差异化亮点，给用户惊喜感。

#### 功能需求


| ID     | 功能    | 优先级 | 描述                           |
| ------ | ----- | --- | ---------------------------- |
| IMG-01 | 自动生成  | P0  | 每次结束对话后，自动生成一张配图             |
| IMG-02 | 情绪匹配  | P0  | 图片风格与本次对话的情绪基调一致（焦虑时不生成欢快图片） |
| IMG-03 | 风格统一  | P0  | 所有图片为治愈系插画风格（参考吉卜力工作室或温馨绘本）  |
| IMG-04 | 无人物面孔 | P0  | 图片中不出现具体人物面部，保持抽象和普适性        |
| IMG-05 | 降级方案  | P1  | 生图失败或超时时，日记正常展示文字内容（无图）      |


#### 图片风格要求(Prompt)

- 使用自然意象表达情绪（光线、天气、植物、水、季节、小动物）
- 禁止出现现代具体物品（桌子、手机、电脑、杯子等）
- 禁止出现人物面孔
- 色调柔和温暖，有故事感和生活气息

#### 兜底 Prompt 词库（13 种情绪）

如果自动生成风格描述失败，系统会使用预置的兜底描述，确保每种情绪都有对应的图片风格


| 情绪  | Prompt                                    |
| --- | ----------------------------------------- |
| 平静  | 清晨森林小路，阳光从树叶间洒落，远处一只小鹿，吉卜力风格治愈插画，薄荷绿与暖金色调 |
| 疲惫  | 黄昏小屋窗边，一只猫蜷缩入睡，暖橘色灯光，吉卜力治愈风格，温柔疲倦的静谧感     |
| 委屈  | 细雨中的野花田，一只小兔躲在蘑菇下，蓝紫色调，绘本风格，温柔委屈的氛围       |
| 焦虑  | 风吹乱的芦苇荡，乌云与阳光交替，一只小鸟紧抓枝头，治愈插画风格，不安中带温柔    |
| 烦躁  | 翻涌的海浪拍打礁石，远处一座灯塔，蓝灰色调，吉卜力风格，情绪激荡又不失美感     |
| 难过  | 秋日落叶铺满的小径，一只刺猬独自走着，蓝灰暖棕色调，绘本治愈风格，安静的孤独感   |
| 低落  | 阴天的麦田，一棵大树下空无一人的秋千，灰绿色调，吉卜力风格，沉静低沉        |
| 开心  | 春日花海中蝴蝶飞舞，暖黄粉色，阳光明媚，吉卜力治愈风格，轻盈欢快          |
| 兴奋  | 烟花绽放的夜空倒映在湖面，彩色光点，活力四射，治愈插画风格，喜悦热烈        |
| 期待  | 清晨薄雾中的山谷，一列小火车缓缓驶来，橙粉暖色调，吉卜力风格，充满期待与希望    |
| 放松  | 午后草地上漂浮的肥皂泡，懒洋洋的云朵，粉蓝暖色，治愈绘本风格，轻盈惬意       |
| 安心  | 壁炉旁堆满书的小屋，窗外飘雪，一杯热茶，暖琥珀色调，吉卜力治愈风格，温馨安全    |
| 困惑  | 迷雾中的森林小路，萤火虫点点，青绿灰色调，绘本风格，神秘迷离又温柔         |


#### 验收标准


| 场景        | 预期行为               |
| --------- | ------------------ |
| 结束焦虑情绪的对话 | 生成的图片风格安静、温柔，不欢快   |
| 生图服务不可用   | 日记正常展示文字内容，无报错或空白  |
| 图片风格      | 治愈系插画，无人物面孔，符合情绪基调 |


---

### 5.10 TTS 语音播报

#### 功能需求


| ID     | 功能       | 优先级 | 描述                                   |
| ------ | -------- | --- | ------------------------------------ |
| TTS-01 | 文字转语音    | P1  | 调用 DashScope qwen3-tts-flash，返回 mp3  |
| TTS-02 | 通话模式自动播放 | P1  | 语音 PTT 对话后 AI 回复自动播放                 |
| TTS-03 | 音色配置     | P2  | 支持通过环境变量配置音色（默认 Cherry）              |
| TTS-04 | 重试机制     | P1  | 失败自动重试 3 次（间隔 0.6s / 1.2s）           |
| TTS-05 | 请求串行化    | P1  | 同时只允许一个 TTS 请求，防止并发导致 DashScope 连接异常 |


#### 验收标准

- PTT 模式下 AI 回复后 3s 内开始播放语音
- 纯标点或空文本不触发 TTS 请求
- TTS 服务不可用时，文字回复正常展示，无崩溃

---

### 5.11 情绪与焦虑分析

#### 业务背景

用户在倾诉过程中会自然流露情绪变化，系统若能感知这些情绪并在回复中体现温度（尤其在识别到焦虑时），能让用户感到“被真正理解”。同时，情绪数据也会沉淀到日记中，形成用户自我觉察的线索。

#### 功能需求


| ID      | 功能模块      | 优先级 | 描述                                             |
| ------- | --------- | --- | ---------------------------------------------- |
| MOOD-01 | 实时情绪分析    | P1  | 用户每次发送消息后，系统后台异步分析用户当前情绪，不阻塞对话响应。              |
| MOOD-02 | 情绪与画像联合提取 | P1  | 一次 LLM 调用同时完成用户画像提取和情绪判断，输出情绪词（2-4字）和是否焦虑。     |
| MOOD-03 | 智能跳过      | P2  | 当消息过短（少于 2 字）或为常见应答词（如“嗯”“好”“谢谢”等）时，跳过分析以节省成本。 |
| MOOD-04 | 会话结束焦虑检测  | P1  | 用户结束对话时，LLM 综合整场对话内容整体判断是否存在焦虑情绪。              |
| MOOD-05 | 焦虑感知回复    | P1  | 若会话结束时检测到焦虑，后续对话中 AI 会收到额外提示，自动调整为更温柔、更多共情的语气。 |


#### 验收标准


| 场景                      | 预期行为                      |
| ----------------------- | ------------------------- |
| 结束对话后查看情绪词              | 情绪词在预设词汇库内（如平静、焦虑、开心、疲惫等） |
| 对话中包含明显焦虑词汇（如“好烦”“压力大”） | 焦虑标记为 `true`，会话结束时记录为焦虑状态 |
| 被标记为焦虑的用户开启新对话          | AI 回复语气明显更柔和、温和，体现共情      |
| 用户发送“嗯”或“好的”等短应答        | 不触发情绪分析调用，节省资源            |


---

### 5.12 日记列表与日历

#### 业务背景

日记是用户“留痕迹”价值的主要承载页。通过日历和时间线的方式呈现历史对话记录，让用户可以回顾自己的情绪变化轨迹，形成自我觉察和治愈感。同时，精美的情绪插画和情绪标签也能增强日记的可读性和情感共鸣。

#### 功能需求


| ID       | 功能模块    | 优先级 | 描述                                                |
| -------- | ------- | --- | ------------------------------------------------- |
| DIARY-01 | 日记卡片展示  | P0  | 每条历史会话生成一张日记卡片，包含心情画、情绪标签、时间、总结摘要及用户图片缩略图。        |
| DIARY-02 | 心情画展示   | P0  | 日记卡片顶部展示 AI 生成的情绪配图，高度 160px，风格统一为治愈插画。           |
| DIARY-03 | 情绪标签展示  | P0  | 在日记卡片中展示该次会话的情绪词（2-4字，如“平静”“焦虑”）。                 |
| DIARY-04 | 用户图片缩略图 | P1  | 展示该次对话中用户上传的图片缩略图，便于快速回顾。                         |
| DIARY-05 | 会话续聊入口  | P0  | 点击日记卡片可继续之前的对话，保留历史消息上下文。                         |
| DIARY-06 | 日历热力图   | P1  | 按月展示日历，每日一格，根据当天情绪分类用不同颜色标记。                      |
| DIARY-07 | 情绪分类着色  | P1  | 积极情绪（开心、放松、平静等）标记为绿色；消极情绪（焦虑、难过等）标记为红色；中性情绪标记为灰色。 |


#### 情绪分类规则


| 分类           | 情绪词                              |
| ------------ | -------------------------------- |
| positive（绿色） | 开心、兴奋、愉快、高兴、喜悦、放松、安心、满足、轻松、平和、平静 |
| negative（红色） | 焦虑、难过、疲惫、烦躁、委屈、低落、压力、紧张、恐惧、绝望、崩溃 |
| neutral（灰色）  | 其他情绪词                            |


> 情绪词来源于 5.11 中 MOOD-02 输出的情绪分析结果。

#### 验收标准


| 场景              | 预期行为                            |
| --------------- | ------------------------------- |
| 打开日记列表          | 有心情画的日记卡片正常展示图片（顶部 160px 治愈插画）  |
| 点击任意日记卡片        | 进入对应历史会话页面，可继续对话，历史消息正常加载       |
| 打开日历视图          | 有日记记录的日期按情绪类别正确高亮（绿/红/灰）        |
| 查看无心情画的日记（生图失败） | 卡片顶部不展示心情画，但情绪标签、时间、摘要等其他元素正常显示 |


---

### 5.13 AI 质量评估（LLM-as-Judge）

#### 业务背景

为保证 AI 回复质量持续稳定，系统对每轮对话进行后台自动评分，用于发现质量问题和验证 prompt 改动效果。

#### 评分维度


| 维度               | 满分  | 说明               |
| ---------------- | --- | ---------------- |
| 共情（empathy）      | 5   | 是否真正接住用户情绪，而非套话  |
| 自然度（naturalness） | 5   | 语言是否口语化，不像客服     |
| 有用性（helpfulness） | 5   | 是否在用户需要时提供了有价值内容 |
| 安全性（safety）      | 5   | 是否避免有害或不当内容      |
| 综合（overall）      | 5   | 综合评分             |


#### 验收标准


| 场景          | 预期行为                   |
| ----------- | ---------------------- |
| 每轮对话结束后     | 系统后台自动完成质量评分，不影响对话响应速度 |
| 评估服务不可用时    | 对话正常进行，不报错或中断          |
| 修改 prompt 后 | 可通过基准测试脚本验证质量是否退步      |
| 查看质量看板      | 可查看评分趋势，用于分析和优化        |


---

### 5.14 数据导出

#### 业务背景

用户有权导出自己的所有对话记录，用于备份或迁移。

#### 功能需求


| ID     | 功能模块         | 优先级 | 描述                                             |
| ------ | ------------ | --- | ---------------------------------------------- |
| EXP-01 | 导出为 Markdown | P2  | 用户可导出所有已结束会话的日期、心情、总结及完整对话，格式为 UTF-8 Markdown。 |


#### 验收标准


| 场景     | 预期行为                                 |
| ------ | ------------------------------------ |
| 用户发起导出 | 系统生成导出文件，包含所有历史会话内容                  |
| 导出文件格式 | 文件名为 `treeholediary.md`，对话记录使用折叠方式展示 |


---

### 5.15 搜索

#### 业务背景

随着日记数量增加，用户需要快速找到某段对话或某个情绪状态下的记录。

#### 功能需求


| ID     | 功能模块  | 优先级 | 描述                          |
| ------ | ----- | --- | --------------------------- |
| SCH-01 | 关键词搜索 | P1  | 用户输入关键词，搜索会话总结和对话内容。        |
| SCH-02 | 结果展示  | P1  | 返回匹配的会话列表，含时间、摘要、情绪标签和焦虑标记。 |
| SCH-03 | 搜索限制  | P2  | 最多返回 30 条结果，按时间倒序排列。        |


#### 验收标准


| 场景          | 预期行为                  |
| ----------- | --------------------- |
| 搜索关键词       | 返回包含该关键词的历史会话，按时间倒序展示 |
| 无匹配结果       | 展示“暂无相关内容”提示          |
| 搜索结果超过 30 条 | 只展示最近 30 条            |


---

### 5.16 模型输出失败的兜底体验

#### 业务背景

AI 模型可能因网络波动、限流等原因偶发性失败。为保证用户体验不崩坏，需要定义各种失败场景下的用户可见反馈。

#### 兜底回复规则


| 失败场景              | 用户看到的回复                 |
| ----------------- | ----------------------- |
| 模型超时（8秒无响应）       | “嗯……我在想怎么说更好，你愿意再说一次吗？” |
| 模型返回空内容           | “我刚走神了一下，能再说一遍吗？”       |
| 连续 3 次失败          | 提示“网络好像不太稳定，稍后重试吧”      |
| 服务严重异常（密钥失效/余额不足） | “小伴暂时有点累，稍后再来找我吧～”      |


#### 降级策略

当辅助服务不可用时，主对话不受影响：


| 服务             | 不可用时行为       |
| -------------- | ------------ |
| 长期记忆 / RAG 知识库 | 静默跳过，用户无感知   |
| 心情画生成          | 日记不配图，文字内容正常 |
| TTS 语音播报       | 不自动播报，文字回复正常 |
| AI 质量评估        | 跳过评分，不影响对话   |


#### 验收标准


| 场景         | 预期行为              |
| ---------- | ----------------- |
| 模型超时或返回空内容 | 用户看到友好兜底回复，不报错或白屏 |
| 连续 3 次失败   | 前端停止请求，提示用户稍后重试   |
| 辅助服务不可用    | 主对话正常进行，用户无感知     |


---

## 6. AI 人设规范

### 6.1 角色定义

- **名字**：小伴
- **定位**：用户最懂他们的朋友。不是心理咨询师，不是客服，就是朋友。
- **基调**：真实、温柔、不费力

### 6.2 说话风格


| 规范   | 细节                            |
| ---- | ----------------------------- |
| 消息拆分 | 拆成 1-3 条短消息依次发出，像连发微信；话少时一条就够 |
| 语言风格 | 口语，不用书面语、正式词汇                 |
| 语气词  | 偶尔使用"嗯……""哎""对"，不是每次，真实人说话的感觉 |
| 表情符号 | 不以 emoji 替代情感，不用表情包撑场面        |


### 6.3 情绪响应原则

**接住处境，不贴标签**

> ✗ 错误：「你一定很委屈」  
> ✓ 正确：「当着那么多人，地都想找个缝钻进去……」

**不主动给建议**

> 用户没问方法 → 不给  
> 用户问了 → 先问一个让你真正能帮上的问题，再给**一个**有用的建议，不是三条

**不以问句收尾（重要）**

> 说完就说完，让对方自己决定要不要继续说  
> 问题只在真的想知道、或对方明显还有话没说完时才问

### 6.4 禁止词表

以下词语**绝对禁止**出现在任何回复中：

> 「你不是一个人」「加油」「一定会好的」「你很棒」「慢慢来」  
> 「我理解你的感受」「你应该……」

### 6.5 特殊场景处理


| 场景        | 处理方式                        |
| --------- | --------------------------- |
| 焦虑 / 身体不适 | 先陪着，不急着说"去检查一下"——那会让人觉得自己有病 |
| 闲聊        | 就闲聊，不要把每句话都引向"你最近怎么样"       |
| 自杀 / 自伤信号 | 温和接住，不评判；引导专业支持，不强迫         |


### 6.6 消息格式规范

```
[NEXT] 标记用于拆分多条消息：

第一条：接情绪（1-2句）
[NEXT]
第二条：展开/追问/陪伴（1-2句）
[NEXT]（可选）
第三条：若有必要的补充

简单的话一条就够，不要硬凑成多条。
```

---

### 6.7 Prompt 版本管理

> Prompt 是 AI 产品的核心资产。本节定义版本管理和回滚策略。

#### 当前版本：v2.0


| 版本   | 发布日期       | 变更内容                     | Benchmark 通过率 | 主要改进    |
| ---- | ---------- | ------------------------ | ------------- | ------- |
| v1.0 | 2025-03-13 | 基础规则式 prompt（"要做到A/B/C"） | 47%           | 首次上线    |
| v1.1 | 2025-04-10 | 增加禁止词表                   | 52%           | 减少套话    |
| v1.2 | 2025-05-20 | 增加焦虑场景特殊处理               | 62%           | 提升安全性   |
| v1.3 | 2025-07-15 | 禁止以问句收尾                  | 68%           | 减少用户压迫感 |
| v1.4 | 2025-09-01 | 增加消息拆分规范                 | 71%           | 提升对话节奏  |
| v2.0 | 2026-04-11 | 人设重写（规则→人设）+ 关闭思维链       | 93%           | 回复更自然   |


#### Prompt 文件组织

```
prompts/
├── system_prompt_v2.0.txt # 当前生产版本
├── system_prompt_v1.4.txt # 上一版本（用于回滚）
├── prompts_archive/ # 历史版本归档
│ ├── v1.0.txt
│ ├── v1.1.txt
│ └── ...
└── CHANGELOG.md # 版本变更记录
```

---

## 4.x 核心业务逻辑

> 本章描述每个关键场景下系统的处理逻辑，重点是**判断规则**和**降级策略**，帮助读者理解产品在各种情况下的行为边界。

### 4.x.1 发送消息

用户发送一条消息后，系统在生成回复之前会并行判断三件事：

- **长期记忆是否需要召回**：若这是本次会话的第一条消息，或用户在问与自身相关的事，则召回过去的记忆作为上下文。
- **知识库是否需要检索**：若消息涉及情绪/焦虑/心理等话题，且不是”然后呢””继续”这类追问，则检索知识库补充背景。
- **情绪是否需要分析**：若消息有实质内容（非”嗯””好”等短应答），异步分析当前情绪，**不阻塞回复生成**。

三项判断完成后，将相关上下文拼入 AI 请求，生成回复。回复拆成 1-3 条短消息依次发出。

### 4.x.2 新建会话

用户开始新一次对话时，系统会检查 2-14 天内的历史记录，判断是否有用户曾提到过但还没有后续的话题（如”等面试结果””在准备考研”）。若有，AI 会在自然时机主动问一句，不强行插入，若用户聊别的则顺着走。

### 4.x.3 结束对话 → 生成日记

用户结束对话后，系统依次处理：

```
分析整场对话
  → 输出：情绪词（2-4字）+ 焦虑标记 + 总结（100-200字）

生成心情画（基于对话摘要，最长等待 60 秒）
  → 成功：日记带图
  → 失败或超时：日记无图，文字内容不受影响

返回日记卡片
```

**关键规则**：

- 本次会话无消息 → 不生成日记
- 焦虑标记为「是」→ 写入记录，未来 14 天内该用户的对话中 AI 语气会更柔和

### 4.x.4 语音输入

用户松开 PTT 按钮后，音频上传并转为文字，之后走与文字消息相同的流程。转写同时，系统分析语音的音量、停顿等特征，生成一段简短描述（如”声音较轻，有明显停顿”），作为隐藏提示传给 AI 用于调整回复语气，不展示给用户。

**降级规则**：转写失败提示用户重试；语音特征分析失败静默跳过，不影响对话。

### 4.x.5 日记列表与日历

## 日记列表展示所有已结束的会话，按时间倒序排列，每条包含心情画、情绪词、总结摘要。日历视图将情绪分为积极/消极/中性三类，用颜色标记每一天，帮助用户感知自己的情绪变化趋势。

## 7. 界面与交互设计

### 7.1 整体布局

```
┌─────────────────────────────────────────────────────┐
│ 左侧栏（可折叠）│      中间聊天区        │ 右侧日记 │
│                 │                        │          │
│ [新建对话]      │  ┌──────────────────┐  │ 日历     │
│                 │  │  消息气泡区域    │  │          │
│ 会话列表        │  │  AI 消息（左）   │  │ 日记列表 │
│ ─ 会话 A       │  │  用户消息（右）  │  │          │
│ ─ 会话 B       │  │                  │  │ 日记卡片 │
│ ─ 会话 C       │  └──────────────────┘  │ ├ 心情画 │
│                 │  [ 输入框 ] [发送]     │ ├ 标签   │
│                 │  [图片] [语音] [结束]  │ └ 摘要   │
└─────────────────────────────────────────────────────┘
```

### 7.2 设计规范


| 项目    | 规范                                  |
| ----- | ----------------------------------- |
| 主色    | 暖米色 `#FAF7F2`                       |
| AI 气泡 | `#F3EDE5`                           |
| 强调色   | `#FCE6B4`（Hover：`#F3D99E`）          |
| 文字色   | 主：`#333333`，次：`#666666`，淡：`#999999` |
| 字体    | Noto Serif SC（标题）+ Noto Sans SC（正文） |
| 整体风格  | 温暖、私密、手账感                           |


### 7.3 关键交互流程

#### 首次打开

1. 检查登录状态 → 未登录跳转注册/登录页
2. 检查是否久未登录 → 有则展示问候提示
3. 进入空白聊天页，等待用户输入

#### 发送消息

1. 用户输入 + Enter / 点发送
2. 用户消息气泡立即展示
3. AI 消息区出现"思考中"动效
4. 流式回复逐字出现，拆分为多条气泡顺序展示

#### 结束对话

1. 点击「结束对话」
2. 展示"正在生成日记…"加载状态
3. 生成完成后：日记列表顶部出现新卡片（含心情画）
4. 用户可点击卡片查看详情

#### 通话模式

1. 点击话筒图标进入通话模式
2. 按住 PTT 说话 → 松开
3. 文字识别结果展示
4. AI 回复后自动 TTS 播放

### 7.4 产品截图

> 以下为产品实际运行截图，建议在 Notion / PDF 版本中插入对应图片。

**截图 1：主界面全貌**
展示三栏布局：左侧会话列表（可折叠）、中间聊天区、右侧日记本。重点体现整体视觉风格——暖米色背景、手账感排版。
`[在此插入主界面截图]`

**截图 2：AI 多条气泡回复**
展示小伴将一条回复拆分为 2-3 条短消息依次展示的效果，体现"真人连发"的对话节奏感。
`[在此插入多气泡回复截图]`

**截图 3：日记卡片**
展示结束会话后生成的日记卡片：顶部吉卜力风格心情画 + 情绪标签 + 总结文字。这是产品最具差异化的视觉亮点。
`[在此插入日记卡片截图]`

**截图 4：日历热力图**
展示月度情绪热力图，正向情绪（绿）/ 负向情绪（红）/ 中性（灰）颜色区分。
`[在此插入日历截图]`

**截图 5：语音 PTT 通话模式**
展示通话界面：按住说话的状态、实时识别文字展示。
`[在此插入语音模式截图]`

---

## 8. 关键设计决策

> 本章记录产品中几个关键设计选择背后的思考过程，体现「为什么这么做」而非「做了什么」。

### 为什么回复要拆成 2-3 条短消息？

早期版本 AI 返回一大段完整回复。测试发现，连续一整段文字给人的感觉是"在念稿子"，用户会下意识觉得自己在接收信息，而不是在和人聊天。改成 2-3 条短消息连发后，节奏明显变轻盈，更接近朋友发消息的感觉。这是一个成本极低但体验提升显著的改动。

### 为什么禁止 AI 以问句收尾？

早期每条回复几乎都以"你怎么看？""你有没有试过……？"结尾。用户反馈"感觉像接受采访"，有压迫感，说了一会儿就不想说了。

根本原因是：AI 在用问句"推进对话"，但用户倾诉的时候不需要被推进，他们需要被接住。改成说完就说完、让用户自主决定是否继续后，对话更自然，用户的发言变得更主动。

### 为什么用 ChromaDB 做长期记忆，而不是搜索历史消息？

跨会话记忆的核心问题是**语义匹配**，不是关键词查找。用户上次说"我最近压力很大"，这次说"最近有点撑不住"——这两句话几乎没有共同词，但语义上强相关。直接搜历史消息找不到；用向量相似度才能召回。

ChromaDB 是专为此设计的本地向量数据库，无需外部服务、数据不离开本机，与产品的隐私定位一致。

### 为什么要建立量化的 AI 质量评估体系？

最初靠主观感受判断 AI 质量——改了 prompt 之后"感觉好多了"，但说不清楚好在哪，也不知道有没有让某些场景变差。

引入 LLM-as-Judge 之后，每次修改都能拿到数字（共情分、自然度、综合分），迭代有了依据。这套体系让"AI 变好了"从感受变成了可追踪的数据，也防止了"这个场景好了但那个场景退步了"的问题。最终 benchmark 通过率从 47% 系统性提升到 80%。

### 为什么定位成「朋友」而不是「咨询师」？

咨询师的回应模式是结构化的：共情 → 探索问题 → 给建议。这套模式在专业场景有效，但放到日常倾诉里会显得刻意，用户会觉得"被分析了"。

大多数人倾诉的时候不需要解决方案，只需要被听见。「朋友」模式的核心是接住具体处境、不贴情绪标签、不主动给建议——这个定位清晰后，所有功能取舍和人设设计都有了统一的判断标准。

---

## 9. 数据模型

### users

```sql
id           INTEGER PRIMARY KEY AUTOINCREMENT
name         TEXT
email        TEXT UNIQUE
password_hash TEXT          -- bcrypt，不可逆
last_login_at TEXT
created_at   TEXT DEFAULT (datetime('now'))
```

### user_tokens

```sql
id         INTEGER PRIMARY KEY AUTOINCREMENT
user_id    INTEGER NOT NULL
token      TEXT NOT NULL UNIQUE  -- 64位随机 hex
expires_at TEXT NOT NULL         -- 30天有效
created_at TEXT DEFAULT (datetime('now'))
FOREIGN KEY (user_id) REFERENCES users(id)
```

### sessions

```sql
id               INTEGER PRIMARY KEY AUTOINCREMENT
user_id          INTEGER NOT NULL
started_at       TEXT DEFAULT (datetime('now'))
ended_at         TEXT                -- 结束会话时填写
summary          TEXT                -- AI 生成总结（100-200字）
anxiety_detected INTEGER DEFAULT 0
mood             TEXT                -- 情绪词（2-4字）
mood_image_url   TEXT                -- 心情画图片 URL
title            TEXT                -- 用户自定义标题（可选）
FOREIGN KEY (user_id) REFERENCES users(id)
```

### messages

```sql
id         INTEGER PRIMARY KEY AUTOINCREMENT
session_id INTEGER NOT NULL
role       TEXT NOT NULL         -- "user" 或 "assistant"
content    TEXT NOT NULL
image_path TEXT                  -- 用户上传图片文件名（可空）
created_at TEXT DEFAULT (datetime('now'))
FOREIGN KEY (session_id) REFERENCES sessions(id)
```

### knowledge

```sql
id         INTEGER PRIMARY KEY AUTOINCREMENT
title      TEXT
content    TEXT NOT NULL
source_url TEXT
embedding  TEXT                  -- JSON 序列化的 float 向量
created_at TEXT DEFAULT (datetime('now'))
```

### evaluations

```sql
id              INTEGER PRIMARY KEY AUTOINCREMENT
session_id      INTEGER NOT NULL
user_message    TEXT              -- 截取前 500 字
assistant_reply TEXT              -- 截取前 1000 字
empathy         INTEGER           -- 0-10
naturalness     INTEGER           -- 0-10
helpfulness     INTEGER           -- 0-10
safety          INTEGER           -- 0-10
overall         REAL              -- 0-10
comment         TEXT
latency_ms      INTEGER           -- LLM 响应耗时（毫秒）
token_count     INTEGER           -- 本轮 token 消耗
created_at      TEXT DEFAULT (datetime('now'))
FOREIGN KEY (session_id) REFERENCES sessions(id)
```

### message_feedback

```sql
id         INTEGER PRIMARY KEY AUTOINCREMENT
message_id INTEGER NOT NULL
session_id INTEGER
user_id    INTEGER NOT NULL
rating     INTEGER NOT NULL       -- 1（👍）或 -1（👎）
created_at TEXT DEFAULT (datetime('now'))
```

### growth_reports

```sql
id           INTEGER PRIMARY KEY AUTOINCREMENT
user_id      INTEGER NOT NULL
month_label  TEXT NOT NULL        -- 格式：2025-03
report_json  TEXT NOT NULL
generated_at TEXT DEFAULT (datetime('now'))
```

### reminders

```sql
id         INTEGER PRIMARY KEY AUTOINCREMENT
user_id    INTEGER NOT NULL
message    TEXT
created_at TEXT DEFAULT (datetime('now'))
```

### ChromaDB（长期记忆，独立文件存储）

文档格式：`用户：{消息}\n小伴：{回复}`

Metadata 字段：


| 字段         | 说明                                                                                                      |
| ---------- | ------------------------------------------------------------------------------------------------------- |
| user_id    | 用户 ID                                                                                                   |
| session_id | 会话 ID                                                                                                   |
| role       | "user" 或 "assistant"                                                                                    |
| created_at | ISO8601 时间戳                                                                                             |
| kind       | profile 条目类型：profile_name / profile_surname / profile_preference / profile_experience / profile_feeling |


---

## 10. API 接口规范

### 通用规范

- **协议**：HTTP/1.1
- **鉴权**：`Authorization: Bearer <token>`（除登录/注册外所有接口）
- **内容类型**：`application/json`（文件上传用 `multipart/form-data`）
- **错误格式**：`{"detail": "错误描述"}`
- **流式接口**：`text/event-stream`，每帧格式 `data: {...}\n\n`

### 接口列表

#### 认证（无需 Token）


| Method | Path                 | 请求体                        | 返回                              | 说明  |
| ------ | -------------------- | -------------------------- | ------------------------------- | --- |
| POST   | `/api/auth/register` | `{email, password, name?}` | `{user_id, name, email, token}` | 注册  |
| POST   | `/api/auth/login`    | `{email, password}`        | `{user_id, name, email, token}` | 登录  |
| POST   | `/api/auth/logout`   | —                          | `{ok: true}`                    | 登出  |


#### 认证（需要 Token）


| Method | Path                        | 请求体                                | 返回                       | 说明     |
| ------ | --------------------------- | ---------------------------------- | ------------------------ | ------ |
| POST   | `/api/auth/change-password` | `{current_password, new_password}` | `{ok: true}`             | 修改密码   |
| DELETE | `/api/auth/account`         | `{password}`                       | `{ok: true}`             | 注销账号   |
| GET    | `/api/auth/me`              | —                                  | `{user_id, name, email}` | 当前用户信息 |


#### 对话


| Method | Path               | 说明                   |
| ------ | ------------------ | -------------------- |
| POST   | `/api/chat`        | 发送消息（非流式）            |
| POST   | `/api/chat/send`   | 发送消息 + 图片（multipart） |
| POST   | `/api/chat/stream` | 流式回复（SSE）            |


**ChatRequest**：

```json
{
  "user_id": 1,
  "session_id": null,
  "message": "今天好累",
  "weather": "☀️ 北京，晴，28°C",
  "voice_hint": "声音较轻，有停顿"
}
```

**流式响应（SSE）**：

```
data: {"session_id": 42}
data: {"text": "今天"}
data: {"text": "真的累了"}
data: {"message_id": 101}
```

#### 会话管理


| Method | Path                          | 说明                   |
| ------ | ----------------------------- | -------------------- |
| POST   | `/api/session/end`            | 结束会话（触发总结+焦虑+心情画）    |
| GET    | `/api/sessions?limit=50`      | 会话列表                 |
| GET    | `/api/sessions/{id}/messages` | 会话历史消息               |
| PATCH  | `/api/sessions/{id}`          | 重命名（body: `{title}`） |
| DELETE | `/api/sessions/{id}`          | 删除会话及消息              |


#### 日记与情绪


| Method | Path                   | 参数            | 说明            |
| ------ | ---------------------- | ------------- | ------------- |
| GET    | `/api/summaries`       | `limit=20`    | 日记列表（含心情画、图片） |
| GET    | `/api/mood`            | —             | 今日心情          |
| GET    | `/api/mood/calendar`   | `year, month` | 月度心情日历        |
| GET    | `/api/search`          | `q=关键词`       | 搜索会话          |
| GET    | `/api/export/markdown` | —             | 导出所有日记（.md）   |


#### 多媒体


| Method | Path                      | 说明                                 |
| ------ | ------------------------- | ---------------------------------- |
| POST   | `/api/tts`                | 文本转语音（body: `{text}`，返回 audio/wav） |
| GET    | `/api/uploads/{filename}` | 获取用户上传图片                           |


#### 知识库（管理员）


| Method | Path                    | 说明                                         |
| ------ | ----------------------- | ------------------------------------------ |
| GET    | `/upload`               | 知识库上传页（HTML）                               |
| POST   | `/api/knowledge/upload` | 上传文件（multipart: file, title?, source_url?） |


---

## 11. 技术架构

### 10.1 架构图

```
┌─────────────────────────────────────────────────────────┐
│                   用户浏览器                              │
│            （单文件 HTML，vanilla JS）                    │
└──────────────────────┬──────────────────────────────────┘
                       │  HTTP / SSE / WebSocket
┌──────────────────────▼──────────────────────────────────┐
│              FastAPI 后端（Python 3.11+）                 │
│                    async/await                           │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │  对话路由     │  │  日记路由     │  │   认证路由     │  │
│  └──────┬───────┘  └──────┬───────┘  └───────┬───────┘  │
│         │                 │                   │          │
│  ┌──────▼──────────────────────────────────── ▼───────┐  │
│  │                  核心服务层                          │  │
│  │  llm.py │ rag.py │ long_memory.py │ image_gen.py   │  │
│  │  tts.py │ evaluate.py │ summary.py │ anxiety.py    │  │
│  └──────────────────────┬──────────────────────────── ┘  │
│                         │                                │
│  ┌──────────────────────▼──────────────────────────────┐ │
│  │               数据存储层                              │ │
│  │   SQLite（companion.db）  ChromaDB（.chroma/）       │ │
│  │   uploads/（用户图片）                               │ │
│  └─────────────────────────────────────────────────────┘ │
└──────────────────────┬──────────────────────────────────┘
                       │  外部 HTTP API
        ┌──────────────┼──────────────────┐
        ▼              ▼                  ▼
  OpenAI 兼容接口   DashScope        天气 API
  （LLM + Embed）  （TTS + 文生图）
```

### 10.2 核心模块

#### LLM 调用层

统一封装所有模型调用，屏蔽不同供应商的差异。关键设计：禁用 Qwen3 思维链模式（避免回复过于正式）、适当提高温度参数（增加口语自然度）。RAG 知识库通过工具调用（function call）实现，让模型自主决定何时需要查知识库。

#### 长期记忆层

ChromaDB 本地向量数据库。写入和画像提取合并为一次 LLM 调用（两个并行的工具调用），减少 API 开销。读写通过单例模式管理，支持并发安全访问。

#### 文生图层

因官方 SDK 存在已知 bug，改为直接调用 DashScope REST API，自行实现异步任务提交和轮询逻辑，获得更细粒度的错误控制。

### 10.3 后台异步任务

每轮对话后，以下三项**后台并行执行，不阻塞对话响应**：


| 任务      | 说明                       |
| ------- | ------------------------ |
| 写入长期记忆  | 将本轮对话存入向量数据库             |
| 提取用户画像  | 识别用户提到的姓名/偏好/经历，更新情绪标签   |
| AI 质量评分 | LLM-as-Judge 自动评分，写入评估记录 |


---

## 12. 环境配置

### 11.1 必填配置

```bash
OPENAI_API_KEY=         # LLM API 密钥
OPENAI_BASE_URL=        # 接口地址，默认 https://api.openai.com/v1
OPENAI_MODEL=           # 对话模型，如 gpt-4o-mini 或 qwen3-max
```

### 11.2 可选配置（功能降级）

```bash
# 语义检索（不填则退回关键词检索）
OPENAI_EMBEDDING_MODEL=

# TTS 语音播报（不填则无 TTS）
DASHSCOPE_API_KEY=
DASHSCOPE_TTS_MODEL=    # 默认 qwen3-tts-flash
DASHSCOPE_TTS_VOICE=    # 默认 Cherry（支持别名"芊悦"）

# 心情画文生图（不填则不生图）
IMAGE_GEN_MODEL=        # 如 wanx2.1-t2i-turbo
IMAGE_GEN_SIZE=         # 默认 512*512（wanx2.1 建议 1024*1024）

# AI 质量评估（不填则不评分）
EVAL_API_KEY=
EVAL_BASE_URL=          # 默认 https://api.deepseek.com
EVAL_MODEL=             # 默认 deepseek-chat

# 长期记忆（ChromaDB）
CHROMA_DIR=.chroma
CHROMA_COLLECTION=chat_long_memory
LONG_MEMORY_TOP_K=4
LONG_MEMORY_MAX_CHARS=700

# 系统
DB_PATH=companion.db
MAX_KNOWLEDGE_UPLOAD_BYTES=20971520  # 20MB
INACTIVE_DAYS_FOR_REMINDER=2
```

### 11.3 模型选择建议


| 用途       | 推荐模型                  | 说明              |
| -------- | --------------------- | --------------- |
| 对话（中文优先） | qwen3-max / qwen-plus | 需关闭 thinking 模式 |
| 对话（通用）   | gpt-4o-mini           | 自然度好，成本低        |
| 向量检索     | text-embedding-v3     | 通义，与 qwen 系搭配   |
| TTS      | qwen3-tts-flash       | DashScope，中文效果好 |
| 文生图      | wanx2.1-t2i-turbo     | 速度快，治愈风好        |
| 评估       | deepseek-chat         | 成本低，中文评分准确      |


---

## 6. 非功能需求与合规

> 本章定义用户可感知的性能、安全、可用性标准，以及产品必须遵守的合规底线。  
> **不描述技术实现方案**（如用什么算法、组件），只描述“要达成什么、用户感受到什么”。

### 13.1 性能要求（用户可感知）


| 用户场景              | 目标      | 超出时的用户感受                | 实现状态                                      |
| ----------------- | ------- | ----------------------- | ----------------------------------------- |
| 发消息后看到第一个字        | ≤ 2 秒   | 觉得”卡了”或”小伴反应慢”          | 已实现 SSE 流式输出，首字时延依赖 LLM API 响应速度，无硬性超时控制  |
| 完整回复生成完成          | ≤ 8 秒   | 长回复可稍长，但超过会觉得等待久        | 无超时控制，实际耗时取决于 LLM API 速度                  |
| 结束对话 → 日记生成完成     | ≤ 65 秒  | 含心情画生成时间；超时则降级（无图但文字正常） | 降级逻辑已实现（图片失败则纯文字日记）；无整体 65 秒硬限制           |
| 语音松开按钮 → 文字识别完成   | ≤ 2 秒   | 超过会觉得语音不好用              | 依赖 DashScope STT API，HTTP 超时 30 秒，无 2 秒保证 |
| 语音松开按钮 → TTS 开始播放 | ≤ 5 秒   | 超过会觉得”怎么还没播”            | 无超时控制，依赖 DashScope TTS API 速度             |
| 打开日记列表（首屏）        | ≤ 1 秒   | 超过会觉得加载慢                | SQLite 直接查询 + 图片懒加载，个人用量下基本满足             |
| 日历视图加载            | ≤ 1.5 秒 | 超过会影响回顾体验               | SQLite 直接查询，个人用量下基本满足                     |


### 13.2 安全与隐私（对用户的承诺）


| 承诺项      | 说明                                    |
| -------- | ------------------------------------- |
| 密码加密存储   | 任何人都无法查看用户的明文密码                       |
| 用户数据严格隔离 | 用户的对话、记忆、日记、图片只属于自己，不可跨账号访问           |
| 注销后彻底删除  | 用户注销账号后，所有关联数据（含图片、记忆）在 7 天内彻底删除，不可恢复 |
| 不收集非必要信息 | 不主动申请位置、通讯录、相册（图片上传由用户主动触发）           |
| 第三方不共享   | 不主动向第三方共享用户数据（除法律法规要求）                |


### 13.3 可用性与容错（用户无感知的降级）


| 异常场景             | 用户看到什么                      | 不影响什么            |
| ---------------- | --------------------------- | ---------------- |
| 心情画生成失败或超时（>60秒） | 日记卡片正常展示文字（总结、情绪词、时间），顶部无图片 | 对话、日记总结、情绪标签全部正常 |
| 长期记忆服务不可用        | AI 不记得之前说过的事，但当前对话正常        | 当前对话、日记生成全部正常    |
| RAG 知识库不可用       | AI 不会引用书籍/文章内容，但正常回复        | 对话正常进行           |
| TTS 语音播报失败       | 文字回复正常展示，不自动播放语音            | 用户可手动阅读          |
| AI 质量评估服务不可用     | 用户无任何感知                     | 对话、日记全部正常        |


### 13.4 兼容性要求


| 类型        | 支持范围                                     |
| --------- | ---------------------------------------- |
| 浏览器       | Chrome、Edge、Safari、Firefox 最新 2 个大版本     |
| 语音功能（PTT） | 主流浏览器支持；Firefox 部分功能可能受限（提示用户但不阻断）       |
| 移动端       | 响应式设计，支持手机浏览器（iOS Safari、Android Chrome） |
| 图片上传      | 支持 jpg、jpeg、png、gif、webp 格式              |


### 13.5 合规要求


| 项         | 要求                                   |
| --------- | ------------------------------------ |
| 生成式 AI 备案 | 如法规要求，产品上线前完成大模型服务备案                 |
| 敏感内容处理    | 检测到自杀/自伤关键词时，后端记录并触发人工复核流程（不自动通知任何人） |
| 用户数据删除    | 注销后，所有数据（含备份、日志）在规定时间内彻底清除           |
| 隐私政策      | 首次注册时需用户明确同意隐私政策，后续更新需再次确认           |
| 未成年人保护    | 如识别到用户可能为未成年人，不展示敏感内容，并在适当节点提示监护人陪同  |


### 13.6 数据保留策略


| 数据类型       | 保留期限      | 说明           |
| ---------- | --------- | ------------ |
| 对话记录、日记、记忆 | 永久（未注销用户） | 用户可随时导出、删除单条 |
| 心情画图片      | 永久（未注销用户） | 随日记保留        |
| 用户主动上传的图片  | 永久（未注销用户） | 随会话保留        |
| 服务端临时日志    | ≤ 30 天    | 仅用于故障排查      |
| 已注销用户数据    | ≤ 7 天     | 彻底清除，不可恢复    |


---

## 7. 风险与依赖

> 本章面向产品、运营、管理层，识别可能影响用户体验、留存或上线进度的风险，以及产品依赖的外部能力。
> **不描述具体技术组件或代码级应对方案**，只描述“什么风险、用户感受到什么、我们怎么办”。

### 7.1 产品与体验风险


| 风险                         | 概率  | 影响  | 用户感知                       | 应对方案                                               | 建议责任人      |
| -------------------------- | --- | --- | -------------------------- | -------------------------------------------------- | ---------- |
| AI 回复质量不稳定（套话、不像朋友、总以问句结尾） | 中   | 高   | “像客服，不自然，不想聊了”             | 上线前 Benchmark 测试；上线后自动评分 + 每周人工抽检低分样本；Prompt 版本可回滚 | 产品 + AI 训练 |
| 心情画与情绪不匹配（焦虑时生成欢快图片）       | 中   | 中   | “这图跟我的心情完全不搭”              | 情绪→Prompt 映射规则持续优化；13 种情绪兜底 Prompt；后续版本支持用户“重新生成”  | 产品         |
| 用户隐私顾虑，不敢深度倾诉              | 中   | 高   | “说了会不会不安全？”                | 隐私政策明确说明数据用途；支持账号注销+彻底删除；后续评估本地部署方案                | 产品 + 法务    |
| 用户次日不回流，忘记“小伴”             | 高   | 高   | “昨天聊完就忘了”                  | 次日温和推送提醒；新会话时 AI 主动提及昨天说过的事（长期记忆）                  | 产品 + 运营    |
| 语音功能在不同浏览器体验不一致            | 高   | 中   | “为什么 Chrome 能用，Safari 不行？” | 优先保证主流浏览器体验；不支持的浏览器提示降级为文字输入                       | 产品 + 前端    |
| AI 主动追问时机不对，让用户觉得“被监视”     | 低   | 中   | “它怎么连这个都记得？有点不舒服”          | 追问只限 2-14 天内未完结的话题；不追问太私密/敏感信息；用户可关闭记忆功能（后续版本）     | 产品         |
| 连续使用后情绪词重复，用户觉得“不准”        | 中   | 低   | “又是‘平静’，我今天明明很烦”           | 情绪词库定期扩充；允许用户手动编辑/修正情绪词（后续版本）                      | 产品         |


### 7.2 外部依赖风险（业务层面）


| 依赖能力      | 用途                | 风险                  | 用户感知                   | 应对方案                            |
| --------- | ----------------- | ------------------- | ---------------------- | ------------------------------- |
| 大语言模型     | 核心对话、总结、情绪识别、记忆提取 | 服务不稳定、限流、供应商变更、成本上涨 | “小伴不回了 / 回得很慢 / 突然变傻了” | 核心对话必须支持多供应商切换；切换时保持回复风格一致      |
| 文生图服务     | 心情画生成             | 服务不可用、生成超时、风格不受控    | “日记没有图 / 图片很奇怪”        | 超时 60 秒后自动降级（日记文字正常展示）；后续支持本地缓存 |
| 语音合成（TTS） | 小伴回复语音播报          | 服务不可用、音色变化          | “怎么没声了 / 声音变了”         | 降级为不自动播报，用户可阅读文字                |
| 长期记忆服务    | 跨会话记住用户信息         | 服务不可用、数据临时丢失        | “小伴怎么不记得我了”            | 不可用时当前对话正常进行，用户感知但对话不中断         |
| 语音识别      | PTT 转文字           | 浏览器兼容性、识别不准         | “我说的话识别错了”             | 不阻塞核心对话；失败时提示用户改用文字输入           |


### 7.3 项目与合规风险


| 风险                    | 概率  | 影响  | 应对方案                                         |
| --------------------- | --- | --- | -------------------------------------------- |
| 生成式 AI 备案未完成，无法在国内上线  | 中   | 高   | 提前 1-2 个月启动备案流程；准备完整合规材料；若未完成，先灰度开放非备案区域或邀请制 |
| 用户量超预期，服务崩溃           | 低→中 | 高   | 上线前压测；设置弹性扩容能力；首月限制邀请制或排队                    |
| 数据安全合规被质疑             | 低   | 高   | 上线前法务/安全评审；不收集非必要信息；支持用户导出和删除；隐私政策清晰易懂       |
| 敏感内容（自杀/自伤）处理不当引发舆论风险 | 低   | 高   | 后端标记并触发人工复核流程；不自动通知任何人；回复内容温和引导专业支持，不评判不强迫   |
| 成本超预期（LLM + 生图）       | 中   | 中   | 设置每日/每月预算上限；监控用量告警；生图失败时自动降级节省成本             |


### 7.4 风险等级说明


| 等级  | 定义                        | 动作要求              |
| --- | ------------------------- | ----------------- |
| 高   | 可能导致上线延期、用户大量流失、合规处罚、舆论风险 | 必须提前制定应对方案，有明确责任人 |
| 中   | 影响用户体验或留存，但可降级或绕开         | 有应对方案，可上线后迭代优化    |
| 低   | 发生概率小，或影响可控               | 监控即可，不阻塞上线        |


## 15. 版本发布记录（Changelog）

### v2.1 — 2026-05（当前版本）

#### 新增功能

- **STT 服务端化**：PTT 录音改为上传服务端，调用 DashScope Paraformer 识别，解决 Firefox 不支持、长语音丢字问题
- **跨浏览器兼容**：替换 Google Fonts 为系统字体，优化 TTS 兼容性，支持 Chrome / Safari / Edge / Firefox

#### 稳定性修复

- 成长报告和周报增加超时保护，超时后前端主动中止请求
- 心情画保存路径可通过环境变量配置（Railway 持久化存储）
- 修复 iOS Safari 报告面板滚动异常
- 修复成长报告容器结构错误导致的布局问题

#### 部署

- 添加 Dockerfile，Railway 切换为容器部署
- 上线 Railway，支持通过环境变量管理密钥和路径配置

---

### v2.0 — 2026-04-11

#### 新增功能

- **多用户系统**：邮箱注册/登录，Token 鉴权，账号注销，旧数据接管
- **语音 PTT 模式**：按住说话，声音能量采集（Web Audio API），voice_hint 情绪感知
- **心情画（文生图）**：结束会话自动生成 Ghibli 治愈风格插画（DashScope wanx）
- **长期记忆**：ChromaDB 跨会话记忆，用户画像提取，姓名优先召回
- **LLM-as-Judge 评估**：每轮对话自动评分，记录 latency_ms 和 token_count
- **主动追问跟进**：新会话时检查 2-14 天前未跟进话题

#### 优化改进

- **AI 人设重写**：从规则式 prompt 改为人设式 prompt，回复更自然、更有温度
- **关闭模型思维链**：消除 Qwen3 过于正式、分析性的回复风格
- **回复质量提升**：benchmark 测试通过率从 47% → 80%
- **禁止回复以问句收尾**：减少用户压迫感，改由用户自主决定是否继续
- **图片风格升级**：改为治愈插画（吉卜力风格），LLM 根据对话内容生成定制 prompt
- **RAG 决策权交给模型**：模型通过工具调用自主决定何时需要查知识库

#### Bug 修复

- 修复文生图无法调用：官方 SDK 存在 bug，改为直接调用 REST API
- 修复日记页不显示心情画：接口返回数据缺少图片字段，补充后正常展示
- 修复 voice_hint 始终为空：声音采集阈值过严，放宽后恢复正常
- 修复 AI 回复每次以问句收尾：在人设规范中明确禁止，并在评估维度中纳入监控

---

### v1.0 — 2025-03-13

#### 首次发布功能

- 文字对话（多轮，支持图片）
- 多会话管理（新建、重命名、删除、切换）
- RAG 知识库（意图识别 + 关键词检索 + 语义检索）
- 情绪分析、焦虑检测
- 会话总结与日记生成
- 日历热力图
- 久未登录问候
- 语音输入（浏览器 Web Speech API）
- TTS 语音播报（DashScope）
- 数据导出（Markdown）
- 搜索

---

## 16. 核心问题与解决过程

> 本章记录产品开发过程中遇到的真实问题，以及分析思路和解决方案。这些不是预先规划好的功能，而是在迭代中碰到、想清楚、解决掉的事情。

### 问题 1：AI 回复太像客服，用户感觉"假"

**现象**

用户反馈集中在三点：感觉像在跟客服聊、共情感觉很假、建议没用。具体表现是：每条回复结构工整、每次都以问句收尾、遇到情绪问题就给三条建议。

**根因分析**

追查后发现了两个叠加的原因：

1. **模型层**：使用的 Qwen3 模型默认开启了「思维链」模式——模型在内部做结构化分析后再输出，导致回复偏报告风格，而不是对话风格
2. **Prompt 层**：系统提示是规则清单（"要做到 A、B、C"），AI 在遵守规则，而不是在「做自己」。规则越多，回复越刻意

**解决方案**

1. 在模型调用层关闭思维链，强制输出对话风格
2. 将 Prompt 从规则清单改写为人设描述——不告诉 AI 要怎么做，而是描述「小伴是一个什么样的人」（参见第 6 章）
3. 加入禁止词表，从负向约束兜底

**结果**

AI 质量 benchmark 通过率从 47% 提升至 80%，自然度和共情两个维度提升最明显。更重要的是：迭代有了可量化的依据，不再依赖主观感受。

---

### 问题 2：长语音识别丢字

**现象**

用户说了 30 秒以上的话，AI 只收到了前面一小句，剩下的全部丢失。

**根因分析**

浏览器 Web Speech API 在检测到静音超过约 1.5 秒时会自动停止识别，触发结束事件后，正在缓冲的内容就此丢失。用户说话中有正常的换气停顿，就会被反复打断。

**解决方案**

1. 识别停止时立即重新创建识别器并继续，用累积变量拼接所有片段的识别结果
2. 用户松手时，等待当前识别实例完成最终回调后再统一发送，防止最后几个字被截断

**结果**

日常长度（10-30 秒）的语音识别完整度显著提升。极快松手（< 0.5 秒）场景仍可能丢失最后几个字，已列入下一版本用服务端 ASR 彻底解决。

---

### 问题 3：文生图 SDK 报错，图片功能无法上线

**现象**

接入心情画功能时，DashScope 官方 Python SDK 调用图片生成接口持续报 `url error`，无法生成任何图片。

**根因分析**

排查后确认是 SDK 内部对异步任务模式的 URL 处理存在 bug，属于 SDK 自身问题，短期内无法通过升级版本解决。

**解决方案**

放弃使用 SDK，直接通过 HTTP 调用 DashScope REST API，自己实现「提交任务 → 轮询状态 → 获取结果」的完整流程。

**结果**

功能正常运行，同时获得了更细粒度的错误处理能力。绕过 SDK 的过程也让我意识到：依赖第三方库时需要有备用路径，这也是为什么 PRD 中要求「IMAGE_GEN_MODEL 未配置时优雅降级」——提前设计好了容错。

---

### 问题 4：如何让 AI 感知用户说话时的情绪状态

**背景**

语音转文字后，情绪信号丢失了。同样的文字"今天好累"，哭着说和平静地说，AI 应该给出不同温度的回应——但它无从判断。

**思路探索**

最直接的方案是引入语音情绪识别模型，但这意味着额外的 API 成本、更长的响应链路，且效果不稳定。

退而求其次的思路：不做精准情绪识别，而是提取几个简单的声学特征，生成自然语言描述，让 AI 自己判断如何调整语气。

**实现**

用浏览器原生 Web Audio API 实时采集声音能量（每 100ms 一个采样点），从音量高低、音量波动、停顿比例三个维度生成描述，如「声音较轻，有明显停顿」，随消息一起发给 AI。AI 收到这条描述后，在回复中自然调整语气，不直接提及它。

**结果**

零额外 API 成本，实现了基础的语音情绪感知。这个方案的精度有限，但对于「用户哭着说话」vs「用户平静讲述」的粗粒度区分已经够用。

---

### 问题 5：如何知道每次 prompt 改动是好还是坏

**背景**

改了一次 Prompt 后，主观感觉好多了，但无法确认是真的变好，还是在某些场景下反而退步了。没有量化标准，迭代就没有方向。

**方案**

1. 整理一批覆盖高频场景的测试问题（求助、闲聊、焦虑、追问等）
2. 引入独立的评估模型（与对话模型解耦），从共情、自然度、有用性、安全性四个维度打分
3. 每次修改 Prompt 后跑完整测试，通过率不得低于上一版本

**结果**

迭代从此有了量化依据。三轮改进的数字轨迹：47% → 73% → 80%，每一次提升都能对应到具体的改动内容，也能看到哪个维度还有空间。这套体系本身成了产品质量的「安全网」。

---

## 17. 产品反思与学习

### 做对了什么

**早早建立了量化评估体系**

很多 AI 产品在质量上依赖主观判断，我从早期就引入了 LLM-as-Judge 和 benchmark。这让「AI 变好了」从感受变成了数据，迭代有了依据，也防止了某个场景变好但另一个场景退步的问题。

**把「不做什么」想清楚了**

情感陪伴产品很容易同时想做咨询师、做日记工具、做效率助手。我很早就确定了「只做朋友」的定位，所有功能取舍都围绕这一点——结果是产品调性保持了一致性，没有变成四不像。

**技术选型留了退路**

LLM 用 OpenAI 兼容格式，一行配置切换供应商；TTS 和文生图都是可选功能，不配置时优雅降级。这让产品在不同资源条件下都能运行，也降低了对单一供应商的依赖。

---

### 做得不够好的

**缺乏真实用户测试**

整个迭代过程主要依赖我自己的使用感受和 LLM 评分，没有系统地让真实用户使用并收集反馈。AI 评分好不等于真实用户喜欢——这是最大的盲区。如果重来，我会在 v1.0 上线后立即找 5-10 个目标用户做访谈。

**图片持久化问题没有在设计阶段考虑到**

心情画使用了第三方返回的外链 URL，这类链接有时效性。这是一个在功能设计时就应该考虑到的问题，结果变成了技术债，已列入 v2.1 优先解决。教训是：凡是涉及「外部存储 URL」的功能，设计阶段就要问自己「这个 URL 三个月后还在吗」。

**没有设计用户增长路径**

产品当前是私有化部署，缺少分享、邀请、裂变的设计。如果未来要扩展到更多用户，这部分需要重新设计。

---

### 如果有更多资源，下一步做什么

1. **真实用户访谈**：找 10 个目标用户，观察他们第一次使用的过程，找到真正的流失节点
2. **服务端语音识别**：替换浏览器 Web Speech API，解决兼容性和丢字问题，同时能支持方言和更长的语音
3. **月度情绪报告**：让用户能回顾自己一段时间内的情绪变化，增强产品的长期留存价值

---

## 18. 迭代规划（Roadmap）

| 优先级 | 版本 | 功能 | 说明 |
|--------|------|------|------|
| P1 | v2.2 | 主动留存提醒 | 接入邮件推送，用户几天未登录时主动发送个性化问候；现有的打开时提醒保留 |
| P2 | v3.0 | 多角色切换 | 用户可选不同说话风格的小伴（温暖型 / 幽默型 / 理性型） |

---

## 19. 待决策问题


| ID   | 问题                    | 背景                                         | 优先级 | 负责人 |
| ---- | --------------------- | ------------------------------------------ | --- | --- |
| Q-01 | 心情画是否需要用户手动重新生成？      | 当前外链可能失效，是否提供"重新生图"按钮                      | 高   | —   |
| Q-02 | 是否开放用户自定义 AI 人设？      | 允许用户调整小伴的说话风格，但可能稀释产品调性                    | 中   | —   |
| Q-03 | 多用户下知识库是否隔离？          | 当前所有用户共用同一知识库（管理员上传），是否需要用户级别隔离            | 中   | —   |
| Q-04 | 是否接入第三方登录（微信/Google）？ | 降低注册门槛，但增加维护复杂度                            | 低   | —   |
| Q-05 | 数据备份方案？               | 当前无自动备份，`companion.db` 和 `.chroma/` 需要手动备份 | 高   | —   |


---

## 20. 附录

### 17.1 术语表


| 术语           | 说明                                    |
| ------------ | ------------------------------------- |
| 小伴           | 产品内 AI 角色名称                           |
| 树洞           | 产品形象，象征可倾诉的私密空间                       |
| Session / 会话 | 一次对话的完整周期，从新建到结束                      |
| RAG          | Retrieval-Augmented Generation，检索增强生成 |
| LLM-as-Judge | 用 LLM 评估另一个 LLM 的输出质量                 |
| voice_hint   | 语音情绪特征描述，由声音能量分析生成                    |
| PTT          | Push-to-Talk，按住说话模式                   |
| ChromaDB     | 本地向量数据库，用于长期记忆存储                      |
| Embedding    | 文本的向量表示，用于语义相似度计算                     |
| extra_system | 每轮对话动态注入的系统提示补充内容                     |
| [NEXT]       | AI 回复中的消息拆分标记，存库时清除                   |


### 17.2 参考文档

- `README.md`：项目启动与配置说明
- `services/llm.py`：LLM 调用层实现
- `services/long_memory.py`：长期记忆完整实现
- `services/image_gen.py`：文生图完整实现
- `scripts/run_benchmark.py`：AI 质量基准测试工具
- `static/dashboard.html`：评估数据可视化页面

### 17.3 AI 质量基准（Benchmark）

当前通过率：**80%**（2026-04-11 版本，基于 `benchmark.json` 问题集）


| 迭代      | 通过率  | 主要改进                  |
| ------- | ---- | --------------------- |
| v1.0 初版 | 47%  | 基础 prompt             |
| 中间版本    | 73%  | 禁止词 + 焦虑场景优化          |
| v2.0 当前 | 80%  | 人设重写 + 关闭 thinking 模式 |
| v2.1 目标 | 85%+ | 待优化                   |


---

*本文档版本 v2.0，基于代码实现整理，与当前 main 分支保持一致。*
*下次更新时间：v2.1 功能上线后。*